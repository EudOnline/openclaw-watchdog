#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "[deprecated] scripts/openclaw-watchdog-v2 is deprecated; use scripts/openclaw-watchdog instead." >&2
exec "$REPO_ROOT/scripts/openclaw-watchdog" "$@"
